interface:
  display_name: "Debug AI Writing"
  short_description: "调试中文表达、客户材料故事线、证据关系与整篇覆盖"
  default_prompt: "Use $debug-aiwriting to diagnose and rewrite this Chinese artifact in its actual genre, preserving facts and evidence; pass any required narrative and evidence gates first, then check language, surface AI traces, external boundaries, and full-artifact coverage without inventing specificity or forcing a universal personal voice."

policy:
  allow_implicit_invocation: true
